function ind_bestComp = fcnBESTCOMP(final_cost, final_pos);
% Functiond finds the distance of every cost position from [-maxL/D, 0] and
% outputs a vector of distances. This is then used to find the optimal
% compromise design. 
% 
% Author: Tom Ryan
    
    